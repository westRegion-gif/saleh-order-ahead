'use client';

import { FormEvent, useEffect, useMemo, useState } from 'react';

type Category={id:string;nameAr:string;nameEn?:string|null;isActive:boolean};
type Branch={id:string;nameAr:string;nameEn?:string|null;acceptsOrders:boolean;isActive:boolean};
type BranchProduct={branchId:string;productId:string;isAvailable:boolean;priceOverride?:string|null;soldOutReason?:string|null};
type Product={id:string;sku:string;nameAr:string;nameEn?:string|null;descriptionAr?:string|null;imageUrl?:string|null;basePrice:string;isActive:boolean;category?:Category|null;categoryId?:string|null;branchProducts:BranchProduct[]};
type Tab='dashboard'|'orders'|'products'|'categories'|'modifiers'|'branches'|'availability'|'promotions'|'customers'|'reports'|'settings';

const API=process.env.NEXT_PUBLIC_API_URL||'http://localhost:3000/v1';
const NAV:[Tab,string,string][]=[['dashboard','Dashboard','Overview'],['orders','Live Orders','Kitchen flow'],['products','Menu & Products','Catalog'],['categories','Categories','Menu structure'],['modifiers','Modifiers','Sizes & extras'],['branches','Branches','Stores'],['availability','Availability','Sold out controls'],['promotions','Promotions','Offers'],['customers','Customers','CRM'],['reports','Reports','Sales'],['settings','Settings','System']];

export default function AdminHome(){
 const [token,setToken]=useState(''); const [authReady,setAuthReady]=useState(false); const [loginError,setLoginError]=useState('');
 const [tab,setTab]=useState<Tab>('dashboard'); const [products,setProducts]=useState<Product[]>([]); const [categories,setCategories]=useState<Category[]>([]); const [branches,setBranches]=useState<Branch[]>([]);
 const [selected,setSelected]=useState<Product|null>(null); const [loading,setLoading]=useState(false); const [saving,setSaving]=useState(false); const [message,setMessage]=useState(''); const [uploading,setUploading]=useState(false); const [imageUrl,setImageUrl]=useState('');
 useEffect(()=>{setToken(sessionStorage.getItem('lmtd_admin_token')||'');setAuthReady(true)},[]);
 async function apiFetch(path:string,init:RequestInit={}){const headers=new Headers(init.headers||{});headers.set('Authorization',`Bearer ${token}`);return fetch(`${API}${path}`,{...init,headers})}
 async function load(){if(!token)return;setLoading(true);try{const [p,c,b]=await Promise.all([apiFetch('/admin/catalog/products',{cache:'no-store'}),apiFetch('/admin/catalog/categories',{cache:'no-store'}),apiFetch('/admin/catalog/branches',{cache:'no-store'})]);if(p.status===401||c.status===401||b.status===401){logout();return}if(!p.ok||!c.ok||!b.ok)throw new Error();setProducts(await p.json());setCategories(await c.json());setBranches(await b.json());setMessage('')}catch{setMessage('تعذر الاتصال بخدمة LMTD API.')}finally{setLoading(false)}}
 useEffect(()=>{if(token)load()},[token]);
 async function login(e:FormEvent<HTMLFormElement>){e.preventDefault();setLoginError('');const fd=new FormData(e.currentTarget);const r=await fetch(`${API}/admin/auth/login`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({username:String(fd.get('username')||''),password:String(fd.get('password')||'')})});if(!r.ok){setLoginError('اسم المستخدم أو كلمة المرور غير صحيحة.');return}const data=await r.json();sessionStorage.setItem('lmtd_admin_token',data.accessToken);setToken(data.accessToken)}
 function logout(){sessionStorage.removeItem('lmtd_admin_token');setToken('');setProducts([]);setCategories([]);setBranches([])}
 const active=useMemo(()=>products.filter(x=>x.isActive).length,[products]); const available=useMemo(()=>products.flatMap(x=>x.branchProducts||[]).filter(x=>x.isAvailable).length,[products]);
 function edit(p?:Product){setSelected(p||({} as Product));setImageUrl(p?.imageUrl||'');setTab('products')}
 async function upload(file:File){if(!['image/jpeg','image/png','image/webp'].includes(file.type)){setMessage('استخدم JPG أو PNG أو WEBP.');return}setUploading(true);try{const r=await apiFetch('/admin/media/presign',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({filename:file.name,contentType:file.type})});if(!r.ok)throw new Error();const {uploadUrl,publicUrl}=await r.json();const u=await fetch(uploadUrl,{method:'PUT',headers:{'Content-Type':file.type},body:file});if(!u.ok)throw new Error();setImageUrl(publicUrl);setMessage('تم رفع الصورة. اضغط حفظ المنتج.')}catch{setMessage('تعذر رفع الصورة. تحقق من Object Storage.')}finally{setUploading(false)}}
 async function saveProduct(e:FormEvent<HTMLFormElement>){e.preventDefault();setSaving(true);const fd=new FormData(e.currentTarget);const body={sku:String(fd.get('sku')||''),nameAr:String(fd.get('nameAr')||''),nameEn:String(fd.get('nameEn')||''),descriptionAr:String(fd.get('descriptionAr')||''),imageUrl,basePrice:Number(fd.get('basePrice')||0),categoryId:String(fd.get('categoryId')||'')||undefined,isActive:fd.get('isActive')==='on'};const r=await apiFetch(selected?.id?`/admin/catalog/products/${selected.id}`:'/admin/catalog/products',{method:selected?.id?'PATCH':'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});setSaving(false);if(!r.ok){setMessage('فشل حفظ المنتج.');return}setSelected(null);setImageUrl('');setMessage('تم حفظ المنتج.');await load()}
 async function hide(id:string){if(!confirm('إخفاء المنتج من المنيو؟'))return;const r=await apiFetch(`/admin/catalog/products/${id}`,{method:'DELETE'});if(r.ok)await load()}
 async function addCategory(e:FormEvent<HTMLFormElement>){e.preventDefault();const fd=new FormData(e.currentTarget);const r=await apiFetch('/admin/catalog/categories',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({nameAr:String(fd.get('nameAr')||''),nameEn:String(fd.get('nameEn')||''),isActive:true})});if(r.ok){e.currentTarget.reset();await load()}}
 async function toggle(p:Product,b:Branch){const bp=p.branchProducts?.find(x=>x.branchId===b.id);if(!bp)return;await apiFetch(`/admin/catalog/branches/${b.id}/products/${p.id}`,{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({isAvailable:!bp.isAvailable})});await load()}
 if(!authReady)return null;
 if(!token)return <main className="loginShell"><section className="loginCard"><div className="loginBrand"><b>LMTD</b><span>OWNER CONSOLE</span></div><h1>Welcome back.</h1><p>Sign in to manage LMTD operations.</p><form onSubmit={login}><label>Username<input name="username" autoComplete="username" required/></label><label>Password<input name="password" type="password" autoComplete="current-password" required/></label>{loginError&&<div className="loginError">{loginError}</div>}<button className="dark">Sign in</button></form><small>Protected owner access</small></section></main>;
 return <main className="ops"><aside className="side"><div className="sideBrand"><b>LMTD</b><span>CONTROL</span></div><div className="storePicker"><small>Access</small><strong>Owner</strong><span>All UAE Branches</span></div><nav>{NAV.map(([id,label,sub])=><button key={id} className={tab===id?'on':''} onClick={()=>setTab(id)}><span>{label}</span><small>{sub}</small></button>)}</nav><button className="logout" onClick={logout}>Sign out</button></aside><section className="work"><header className="top"><div><small>LMTD COFFEE</small><strong>{NAV.find(x=>x[0]===tab)?.[1]}</strong></div><div className="topActions"><button onClick={load}>Refresh</button><button className="dark" onClick={()=>edit()}>+ New product</button></div></header><div className="page">{message&&<div className="notice">{message}</div>}
 {tab==='dashboard'&&<><div className="heroAdmin"><div><p>Owner console</p><h1>Store operations at a glance.</h1><span>Orders, menu, branch availability and performance in one place.</span></div><button className="dark" onClick={()=>setTab('orders')}>Open live orders</button></div><div className="kpis"><article><span>Products</span><b>{products.length}</b><small>{active} active</small></article><article><span>Branches</span><b>{branches.length}</b><small>UAE network</small></article><article><span>Available slots</span><b>{available}</b><small>branch × product</small></article><article><span>Orders today</span><b>—</b><small>Orders API next</small></article></div></>}
 {tab==='products'&&<div className="catalogGrid"><div className="card"><div className="cardTitle"><div><h1>Menu & Products</h1><p>Images, prices, names, categories and visibility.</p></div><button className="dark" onClick={()=>edit()}>+ Add item</button></div>{loading?<p>Loading…</p>:<div className="productList">{products.map(p=><article key={p.id}><div className="thumb">{p.imageUrl?<img src={p.imageUrl} alt=""/>:<span>IMG</span>}</div><div className="grow"><b>{p.nameEn||p.nameAr}</b><span>{p.category?.nameAr||'Uncategorized'} · AED {Number(p.basePrice).toFixed(2)}</span><small>{p.sku}</small></div><span className={p.isActive?'pill green':'pill'}>{p.isActive?'Active':'Hidden'}</span><button onClick={()=>edit(p)}>Edit</button><button onClick={()=>hide(p.id)}>Hide</button></article>)}</div>}</div><div className="card editor"><h2>{selected?.id?'Edit product':'New product'}</h2>{!selected?<div className="emptyState">Select an item or add a new product.</div>:<form onSubmit={saveProduct} className="form"><label>Product image<div className="uploadBox">{imageUrl?<img src={imageUrl} alt="preview"/>:<div><b>Upload product image</b><span>JPG, PNG or WEBP</span></div>}<input type="file" accept="image/jpeg,image/png,image/webp" onChange={e=>e.target.files?.[0]&&upload(e.target.files[0])}/></div>{uploading&&<small>Uploading…</small>}</label><div className="two"><label>Arabic name<input name="nameAr" required defaultValue={selected.nameAr||''}/></label><label>English name<input name="nameEn" defaultValue={selected.nameEn||''}/></label></div><div className="two"><label>SKU<input name="sku" required defaultValue={selected.sku||''}/></label><label>Price AED<input name="basePrice" type="number" step="0.01" required defaultValue={selected.basePrice||''}/></label></div><label>Category<select name="categoryId" defaultValue={selected.categoryId||selected.category?.id||''}><option value="">Uncategorized</option>{categories.map(c=><option key={c.id} value={c.id}>{c.nameAr}</option>)}</select></label><label>Description<textarea name="descriptionAr" rows={4} defaultValue={selected.descriptionAr||''}/></label><label className="check"><input type="checkbox" name="isActive" defaultChecked={selected.id?selected.isActive:true}/> Visible in customer menu</label><div className="formBtns"><button type="button" onClick={()=>setSelected(null)}>Cancel</button><button className="dark" disabled={saving}>{saving?'Saving…':'Save product'}</button></div></form>}</div></div>}
 {tab==='categories'&&<div className="card full"><div className="cardTitle"><div><h1>Categories</h1><p>Control customer menu groups.</p></div></div><form className="inline" onSubmit={addCategory}><input name="nameAr" placeholder="Arabic category name" required/><input name="nameEn" placeholder="English category name"/><button className="dark">Add category</button></form><div className="tiles">{categories.map(c=><article key={c.id}><b>{c.nameAr}</b><span>{c.nameEn}</span><small>{c.isActive?'Active':'Hidden'}</small></article>)}</div></div>}
 {tab==='availability'&&<div className="card full"><div className="cardTitle"><div><h1>Availability</h1><p>One-tap sold-out control per branch.</p></div></div>{branches.map(b=><div className="branchBlock" key={b.id}><h2>{b.nameEn||b.nameAr}</h2><div className="availGrid">{products.map(p=>{const bp=p.branchProducts?.find(x=>x.branchId===b.id);const on=bp?.isAvailable!==false;return <button key={p.id} disabled={!bp} className={on?'avail yes':'avail no'} onClick={()=>toggle(p,b)}><span>{p.nameEn||p.nameAr}</span><b>{on?'AVAILABLE':'SOLD OUT'}</b></button>})}</div></div>)}</div>}
 {tab==='branches'&&<div className="card full"><div className="cardTitle"><div><h1>Branches</h1><p>Store status and order controls.</p></div></div><div className="tiles branchTiles">{branches.map(b=><article key={b.id}><span className="pill green">{b.isActive?'Active':'Disabled'}</span><h2>{b.nameEn||b.nameAr}</h2><p>{b.acceptsOrders?'Accepting orders':'Orders paused'}</p></article>)}</div></div>}
 {['orders','modifiers','promotions','customers','reports','settings'].includes(tab)&&<Placeholder title={NAV.find(x=>x[0]===tab)?.[1]||''}/>}</div></section></main>
}
function Placeholder({title}:{title:string}){return <div className="card full"><div className="cardTitle"><div><h1>{title}</h1><p>This workspace is part of the owner console.</p></div></div><div className="emptyState"><b>{title} module</b><span>Backend connection will be added in the next implementation phase.</span></div></div>}
